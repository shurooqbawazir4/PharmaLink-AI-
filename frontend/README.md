# MedCycle AI — Frontend

**Status: not yet implemented — lands in Milestone D.**

Next.js 15 (App Router) + React 19 + TypeScript + TailwindCSS + shadcn/ui +
Framer Motion + TanStack Query + React Hook Form + Recharts + Mapbox.

Planned pages: Dashboard, Medicines, Hospitals, Forecast, Optimization,
Alerts, Procurement, Analytics, Sustainability, AI Assistant — dark mode,
animated, responsive. Talks to the FastAPI backend at
`NEXT_PUBLIC_API_URL` (see `docker/.env.example`).
